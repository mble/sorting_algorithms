sudo: false
cache: bundler
language: ruby
rvm:
  - 2.3.1
  - 2.2.5
  - ruby-head
  - jruby-9.0.5.0
matrix:
  allow_failures:
    - 2.2.5
    - ruby-head
    - jruby-9.0.5.0
  fast_finish: true
before_install: gem install bundler -v 1.12.5
script:
  - bundle exec rake spec
  - bundle exec rake run_benchmarks
